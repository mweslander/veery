import { expect } from 'chai';

describe('App', ()=> {
  it('should prove that my tests are working until I am ready to get this show on the road', function() {
    const veery = 'awesome';
    expect(veery).to.equal('awesome');
  });
});
